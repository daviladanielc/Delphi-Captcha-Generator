unit Captcha.Enum;

interface

type
  TCaptchaType = (ctLetters, ctNumbers, ctAlphaNumeric);
  TLetterCase = (lcUppercase, lcLowercase, lcMixed);
  TDifficulty = (dcEasy, dcModerate, dcHard);

implementation

end.
